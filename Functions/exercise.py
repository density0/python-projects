def myfunc(words):
    up_low = []
    bruh = ''
    for i in bruh:
        if len(bruh) % 2 == 0:

            return up_low.append(bruh)
        else:

            return up_low.append(bruh)

    return words


print(myfunc('elephant'))
